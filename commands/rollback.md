---
description: Safely revert to a previous state via git revert
agent: code-architect
subtask: false
---

Show me the recent git log and help me safely revert to a previous state. Use `git revert` to undo changes — never use `git reset --hard`.
